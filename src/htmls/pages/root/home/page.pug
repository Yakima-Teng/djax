extends ../../../templates/post/template.pug

include ../../../components/mixin-head/component.pug
include ../../../components/mixin-header/component.pug

block site-head
  -var title = ''
  -var desc = ''
  +mixinHead(title, desc)
  link(rel="stylesheet", href="./page.min.css")

block site-header
  +mixinHeader('home')

block site-foot
  script(src="./page.min.js")

block site-content
  .container.page-home
    .jumbotron
      h1 Djax
      p
      p 开箱即用的多页应用脚手架
      p
      p
        a.btn.btn-lg.btn-default(href="javascript:void(0);", role="button") 开始你的表演

    .features
      .row
        .col-md-6
          h2 上手容易
          p 项目架构易于理解，可以直接开干，如果能阅读下gulpfile.js中对于src/scripts/libs目录的处理会更好。
        .col-md-6
          h2 开发方便
          p 使用Gulp构建本地开发环境环境，支持命令行直接部署文件到服务器上。
      .row
        .col-md-6
          h2 节省时间
          p 使用pug和sass，并使用autoprefixer自动补写厂商前缀，免去手写纯html和css的繁琐。
        .col-md-6
          h2 自由灵活
          p 自带了一个作为项目源代码的utils.js工具库，可以直接改成你自己的utils.js。
